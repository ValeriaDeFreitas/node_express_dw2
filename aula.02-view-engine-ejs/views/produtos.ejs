<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Produtos</title>
</head>
<body>
    <%= produtos %>
    <% if(!produto){%>

        <% } %>

    <%if(!produto) { %>
        <% listaProdutos.forEach((produto) => {
            <p><a href="/produtos/<% produto %>"><% produto%></a></p>
        })
        <% } %>
</body>
</html>